//import * as path from 'path';
//import * as assert from 'assert';
//import * as ttm from 'azure-pipelines-task-lib/mock-test';

describe('Sample task tests', function () {
    before(function () {
        // create before
    });

    after(() => {
        // create before
    });

    it('create tests', function (done: Mocha.Done) {
        // create tests
        done();
    });
});
